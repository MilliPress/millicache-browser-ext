# Mozilla Add-ons (AMO) API Credentials
# Get these from: https://addons.mozilla.org/developers/addon/api/key/

# JWT Issuer (looks like: user:12345:67)
AMO_JWT_ISSUER=

# JWT Secret (long hash string)
AMO_JWT_SECRET=

# How to obtain these credentials:
# 1. Visit https://addons.mozilla.org/developers/addon/api/key/
# 2. Log in with your Firefox Account
# 3. Click "Generate new credentials"
# 4. Copy the "JWT issuer" to AMO_JWT_ISSUER above
# 5. Copy the "JWT secret" to AMO_JWT_SECRET above
# 6. Save this file as .env (without .example)
# 7. NEVER commit the .env file to git!
